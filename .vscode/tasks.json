{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile prj", // 自定义任务名称
      "type": "shell",
      "command": "cmake --build ./build", // 你想要执行的命令
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}" // 工作目录
      }
    }
  ]
}